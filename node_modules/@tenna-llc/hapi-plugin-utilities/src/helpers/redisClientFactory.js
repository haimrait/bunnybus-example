'use strict';

const BaseClientFactory = require('./baseClientFactory');

class RedisClientFactory extends BaseClientFactory {
    constructor() {
        super();
    }

    build(name) {
        const Redis = require('ioredis');

        return new Redis(super.get(name));
    }
}

module.exports = RedisClientFactory;

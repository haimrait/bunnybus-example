'use strict';

const Pkg = require('../package.json');
const Plugin = require('./plugin');

const register = async function (server, options) {
    const plugin = new Plugin(server, options);
};

const hapiPlugin = {
    pkg: Pkg,
    register
};

module.exports = hapiPlugin;
